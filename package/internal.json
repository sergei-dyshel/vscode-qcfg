{
  "contributes": {
    "commands": [
      {
        "command": "qcfg.persistentStorage.browseGlobal",
        "title": "qcfg: Browse GLOBAL persistent storage"
      },
      {
        "command": "qcfg.persistentStorage.browseWorkspace",
        "title": "qcfg: Browse WORKSPACE persistent storage"
      }
    ]
  }
}
