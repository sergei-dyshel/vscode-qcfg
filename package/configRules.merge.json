{
  "contributes": {
    "configuration": {
      "properties": {
        "qcfg.configRules": {
          "description": "Array configuration rules per file type, name etc.",
          "$import": "configRules.schema.json"
        }
      }
    }
  }
}
